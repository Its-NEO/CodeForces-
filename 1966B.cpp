#include <iostream>

int main()
{
	int t;
	std::cin >> t;

	for (int i = 0; i < t; i++)
	{
		int n, m;
		std::cin >> n >> m;

		char arr[n][m];
		for (int i = 0; i < n; i++)
			for (int j = 0; j < m; j++)
				std::cin >> arr[n][m];

		int ptr = 0;
		while (ptr < n * m)
		{
			int runner = ptr + 1;
			
		}
	}
}
